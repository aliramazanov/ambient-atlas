# Absolute origin of the deployed site, no trailing slash.
# Canonical links, og:url and og:image are omitted when this is unset, and
# `pnpm data:sitemap` writes nothing rather than guessing a domain.
PUBLIC_SITE_URL=https://your-domain
